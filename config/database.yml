default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: rotary_estoque
  username: postgres
  password: postmy
  host: localhost
  port: 5433

test:
  <<: *default
  database: rotary_estoque
  username: postgres
  password: postmy
  host: localhost
  
production:
  <<: *default
  database: rotary_estoque
  username: postgres
  password: postmy
  host: localhost
  
